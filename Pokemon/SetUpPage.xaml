<Page
    x:Class="Pokemon.SetUpPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pokemon"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel Orientation="Horizontal" >
        <StackPanel.Background>
            <ImageBrush ImageSource="/Assets/setup1.jpg"/>
        </StackPanel.Background>

        <StackPanel Orientation="Horizontal" Height="81" >
            <ComboBox  x:Name="PokemonCombo" Header="Pick a Pokemon" PlaceholderText="Pick a color" Width="298" FontSize="24" Margin="100,0,100,0" ItemsSource="{x:Bind pokemon}">
                <x:String></x:String>
                <x:String>Green</x:String>
                <x:String>Red</x:String>
                <x:String>Yellow</x:String>
            </ComboBox>

        </StackPanel>
        <Button x:Name="LetsBattleBtn"
                Content="Start!"
                Height="50"
                Width="200"
                FontSize="30"
                Foreground="White"
                Background="Black"
                Click="LetsBattleBtn_OnClick"
                />


    </StackPanel>
</Page>
